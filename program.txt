class PI {
    int get_pi(){
        return 3.14;
    },
    PI new_pi(){
        PI rez;
        return rez;
    },
    bool is_pi(float nr){
        bool rez;
        if(nr==3.14){
            rez:=true;
        }
        else{
            rez:=false;
        }
        return rez;
    }
};

class Math
{
    PI pi,
    float e,
    int cmmdc(int a,int b){
        int r;
        while(a*2==2){
            r:=a%b;
            a:=b;
            b:=r;
            if(not b==0 && a>0){
                return a;
            }
        }
        return a;
    }
};

float rez;

float calc(int a,int b, Math m1, Math m2){
    rez := m2.pi.get_pi() + 4.301 - m1.cmmdc(a,b);
    return rez;
};

int a, b;
Math m1,m2;
PI p;

main{
    m2.pi := p.new_pi();
    a := 6;
    f(not not not not ((a+b)>=c) );
    Print(a+n);
    if (not not not (not (a == 1))) {
        Print(a-b);
        Print("Ana are mere multe rau de tot");
    }
    b := 4 + 3;
    f(5);
    rez:=calc(a,b,m1,m2);
}